namespace AjaxControl{
    
    export class AjaxFormSubmit {
        /*
        submitForm( formName : string, successCallback : Function, globfunction : any, onsubmitFunction? : Function ){
            
            if(onsubmitFunction != null) onsubmitFunction();

            document.querySelectorAll( "form[name = "+ formName +"]" ).onsubmit = () => {
                
                let data = new FormData(this);
                

            }

        }
        */
    }

}